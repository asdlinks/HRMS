// Attendance reports category. Every entry shares one of a handful of
// underlying queries in attendanceReports.repository.js — most of these
// differ only by `fixedFilters` (a server-side preset that wins over
// whatever the client sends) or by which subset of the shared columns they
// surface. `attendance-regularization` is intentionally NOT here — it's a
// stub in stubs.registry.js (no regularization-request table exists yet).
const ROW_FILTERS = ['branchId', 'departmentId', 'shiftId', 'workModeId', 'status', 'search', 'dateFrom', 'dateTo'];

const EMPLOYEE_COL = { field: 'employee_id', headerName: 'Emp ID' };
const NAME_COL = { field: 'user_name', headerName: 'Name' };
const DEPT_COL = { field: 'department_name', headerName: 'Department' };
const BRANCH_COL = { field: 'branch_name', headerName: 'Branch' };
const DATE_COL = { field: 'date', headerName: 'Date' };
const CHECK_IN_COL = { field: 'check_in_time', headerName: 'Check In' };
const CHECK_OUT_COL = { field: 'check_out_time', headerName: 'Check Out' };
const STATUS_COL = { field: 'status', headerName: 'Status' };
const WORKED_COL = { field: 'worked_minutes', headerName: 'Worked (min)' };
const LATE_COL = { field: 'late_minutes', headerName: 'Late (min)' };
const WORK_MODE_COL = { field: 'work_mode', headerName: 'Work Mode' };
const SHIFT_COL = { field: 'shift_name', headerName: 'Shift' };

module.exports = [
    {
        id: 'attendance-daily',
        category: 'attendance',
        title: 'Daily Attendance',
        description: 'One row per check-in — defaults to today when no date range is chosen.',
        scopePermissionPrefix: 'reports.attendance.view',
        filters: ROW_FILTERS,
        columns: [EMPLOYEE_COL, NAME_COL, DEPT_COL, BRANCH_COL, DATE_COL, CHECK_IN_COL, CHECK_OUT_COL, STATUS_COL, WORKED_COL, LATE_COL, WORK_MODE_COL, SHIFT_COL],
        defaultSortField: 'date',
        chart: null,
        favoritable: true,
        mode: 'table',
        repoKey: 'attendanceReports',
        fnName: 'listAttendance',
    },
    {
        id: 'attendance-monthly',
        category: 'attendance',
        title: 'Monthly Attendance',
        description: 'Attendance rows across a chosen date range (send dateFrom/dateTo for a full month).',
        scopePermissionPrefix: 'reports.attendance.view',
        filters: ROW_FILTERS,
        columns: [EMPLOYEE_COL, NAME_COL, DEPT_COL, BRANCH_COL, DATE_COL, CHECK_IN_COL, CHECK_OUT_COL, STATUS_COL, WORKED_COL, LATE_COL, WORK_MODE_COL, SHIFT_COL],
        defaultSortField: 'date',
        chart: null,
        favoritable: true,
        mode: 'table',
        repoKey: 'attendanceReports',
        fnName: 'listAttendance',
    },
    {
        id: 'attendance-summary',
        category: 'attendance',
        title: 'Attendance Summary',
        description: 'Per-employee totals (present days, late days, avg worked minutes, overtime) for the filtered range — defaults to month-to-date.',
        scopePermissionPrefix: 'reports.attendance.view',
        filters: ['branchId', 'departmentId', 'managerId', 'search', 'dateFrom', 'dateTo'],
        columns: [
            EMPLOYEE_COL, NAME_COL, DEPT_COL, BRANCH_COL,
            { field: 'present_days', headerName: 'Present Days' },
            { field: 'late_days', headerName: 'Late Days' },
            { field: 'avg_worked_minutes', headerName: 'Avg Worked (min)' },
            { field: 'total_overtime_minutes', headerName: 'Total OT (min)' },
        ],
        defaultSortField: 'user_name',
        chart: null,
        favoritable: true,
        mode: 'table',
        repoKey: 'attendanceReports',
        fnName: 'summarizeAttendanceByEmployee',
    },
    {
        id: 'attendance-late-arrival',
        category: 'attendance',
        title: 'Late Arrival',
        description: 'Attendance rows with recorded late minutes.',
        scopePermissionPrefix: 'reports.attendance.view',
        filters: ['branchId', 'departmentId', 'shiftId', 'workModeId', 'search', 'dateFrom', 'dateTo'],
        columns: [EMPLOYEE_COL, NAME_COL, DEPT_COL, BRANCH_COL, DATE_COL, CHECK_IN_COL, LATE_COL, SHIFT_COL, WORK_MODE_COL],
        defaultSortField: 'date',
        chart: null,
        favoritable: true,
        mode: 'table',
        repoKey: 'attendanceReports',
        fnName: 'listAttendance',
        fixedFilters: { lateOnly: true },
    },
    {
        id: 'attendance-early-exit',
        category: 'attendance',
        title: 'Early Exit',
        description: 'Attendance rows flagged as an early exit.',
        scopePermissionPrefix: 'reports.attendance.view',
        filters: ['branchId', 'departmentId', 'shiftId', 'workModeId', 'search', 'dateFrom', 'dateTo'],
        columns: [EMPLOYEE_COL, NAME_COL, DEPT_COL, BRANCH_COL, DATE_COL, CHECK_OUT_COL, WORKED_COL, SHIFT_COL, WORK_MODE_COL],
        defaultSortField: 'date',
        chart: null,
        favoritable: true,
        mode: 'table',
        repoKey: 'attendanceReports',
        fnName: 'listAttendance',
        fixedFilters: { earlyExitOnly: true },
    },
    {
        id: 'attendance-missing-checkin',
        category: 'attendance',
        title: 'Missing Check In',
        description: 'Active employees with no attendance row at all for a given day (defaults to today; use dateFrom to pick another day).',
        scopePermissionPrefix: 'reports.attendance.view',
        filters: ['branchId', 'departmentId', 'managerId', 'search', 'dateFrom'],
        columns: [EMPLOYEE_COL, NAME_COL, DEPT_COL, BRANCH_COL, DATE_COL],
        defaultSortField: 'user_name',
        chart: null,
        favoritable: true,
        mode: 'table',
        repoKey: 'attendanceReports',
        fnName: 'listMissingCheckIn',
    },
    {
        id: 'attendance-missing-checkout',
        category: 'attendance',
        title: 'Missing Check Out',
        description: 'Past attendance rows with a check-in but no check-out recorded (today\'s still-open shifts are excluded).',
        scopePermissionPrefix: 'reports.attendance.view',
        filters: ['branchId', 'departmentId', 'shiftId', 'workModeId', 'search', 'dateFrom', 'dateTo'],
        columns: [EMPLOYEE_COL, NAME_COL, DEPT_COL, BRANCH_COL, DATE_COL, CHECK_IN_COL, SHIFT_COL, WORK_MODE_COL],
        defaultSortField: 'date',
        chart: null,
        favoritable: true,
        mode: 'table',
        repoKey: 'attendanceReports',
        fnName: 'listMissingCheckOut',
    },
    {
        id: 'attendance-wfh',
        category: 'attendance',
        title: 'Work From Home',
        description: 'Attendance rows recorded under the Work From Home work mode.',
        scopePermissionPrefix: 'reports.attendance.view',
        filters: ['branchId', 'departmentId', 'search', 'dateFrom', 'dateTo'],
        columns: [EMPLOYEE_COL, NAME_COL, DEPT_COL, BRANCH_COL, DATE_COL, CHECK_IN_COL, CHECK_OUT_COL, STATUS_COL, WORKED_COL],
        defaultSortField: 'date',
        chart: null,
        favoritable: true,
        mode: 'table',
        repoKey: 'attendanceReports',
        fnName: 'listAttendance',
        fixedFilters: { workModeCode: 'WFH' },
    },
    {
        id: 'attendance-hybrid',
        category: 'attendance',
        title: 'Hybrid Attendance',
        description: 'Attendance rows recorded under the Hybrid work mode.',
        scopePermissionPrefix: 'reports.attendance.view',
        filters: ['branchId', 'departmentId', 'search', 'dateFrom', 'dateTo'],
        columns: [EMPLOYEE_COL, NAME_COL, DEPT_COL, BRANCH_COL, DATE_COL, CHECK_IN_COL, CHECK_OUT_COL, STATUS_COL, WORKED_COL],
        defaultSortField: 'date',
        chart: null,
        favoritable: true,
        mode: 'table',
        repoKey: 'attendanceReports',
        fnName: 'listAttendance',
        fixedFilters: { workModeCode: 'Hybrid' },
    },
    {
        id: 'attendance-field-visit',
        category: 'attendance',
        title: 'Field Visit',
        description: 'Attendance rows recorded under the Field Work mode, including client and location detail.',
        scopePermissionPrefix: 'reports.attendance.view',
        filters: ['branchId', 'departmentId', 'search', 'dateFrom', 'dateTo'],
        columns: [
            EMPLOYEE_COL, NAME_COL, DEPT_COL, BRANCH_COL, DATE_COL, CHECK_IN_COL, CHECK_OUT_COL,
            { field: 'client_name', headerName: 'Client' },
            { field: 'location_address', headerName: 'Location' },
            STATUS_COL,
        ],
        defaultSortField: 'date',
        chart: null,
        favoritable: true,
        mode: 'table',
        repoKey: 'attendanceReports',
        fnName: 'listAttendance',
        fixedFilters: { workModeCode: 'FieldWork' },
    },
    {
        id: 'attendance-face-recognition',
        category: 'attendance',
        title: 'Face Recognition Attendance',
        description: 'Attendance rows captured via face recognition, including match confidence.',
        scopePermissionPrefix: 'reports.attendance.view',
        filters: ['branchId', 'departmentId', 'search', 'dateFrom', 'dateTo'],
        columns: [
            EMPLOYEE_COL, NAME_COL, DEPT_COL, BRANCH_COL, DATE_COL, CHECK_IN_COL, CHECK_OUT_COL,
            { field: 'confidence', headerName: 'Match Confidence' },
            STATUS_COL,
        ],
        defaultSortField: 'date',
        chart: null,
        favoritable: true,
        mode: 'table',
        repoKey: 'attendanceReports',
        fnName: 'listAttendance',
        fixedFilters: { method: 'Face' },
    },
    {
        id: 'attendance-shift',
        category: 'attendance',
        title: 'Shift Attendance',
        description: 'Attendance rows grouped/filterable by shift.',
        scopePermissionPrefix: 'reports.attendance.view',
        filters: ['branchId', 'departmentId', 'shiftId', 'search', 'dateFrom', 'dateTo'],
        columns: [EMPLOYEE_COL, NAME_COL, DEPT_COL, BRANCH_COL, SHIFT_COL, DATE_COL, CHECK_IN_COL, CHECK_OUT_COL, STATUS_COL, LATE_COL, WORKED_COL],
        defaultSortField: 'date',
        chart: null,
        favoritable: true,
        mode: 'table',
        repoKey: 'attendanceReports',
        fnName: 'listAttendance',
    },
    {
        id: 'attendance-overtime',
        category: 'attendance',
        title: 'Overtime',
        description: 'Attendance rows with recorded overtime minutes.',
        scopePermissionPrefix: 'reports.attendance.view',
        filters: ['branchId', 'departmentId', 'shiftId', 'search', 'dateFrom', 'dateTo'],
        columns: [
            EMPLOYEE_COL, NAME_COL, DEPT_COL, BRANCH_COL, DATE_COL, WORKED_COL,
            { field: 'overtime_minutes', headerName: 'Overtime (min)' },
            { field: 'overtime_hours', headerName: 'Overtime (hrs)' },
            SHIFT_COL,
        ],
        defaultSortField: 'date',
        chart: null,
        favoritable: true,
        mode: 'table',
        repoKey: 'attendanceReports',
        fnName: 'listAttendance',
        fixedFilters: { overtimeOnly: true },
    },
    {
        id: 'attendance-trend',
        category: 'attendance',
        title: 'Attendance Trend',
        description: 'Present/late counts and average worked minutes over time — daily buckets for ranges up to ~2 months, monthly buckets beyond that.',
        scopePermissionPrefix: 'reports.attendance.view',
        filters: ['branchId', 'departmentId', 'shiftId', 'workModeId', 'search', 'dateFrom', 'dateTo'],
        columns: [
            DATE_COL,
            { field: 'present_count', headerName: 'Present' },
            { field: 'late_count', headerName: 'Late' },
            { field: 'avg_worked_minutes', headerName: 'Avg Worked (min)' },
        ],
        defaultSortField: 'date',
        chart: { type: 'line', xField: 'date', yField: 'present_count' },
        favoritable: true,
        mode: 'bespoke',
        repoKey: 'attendanceReports',
        fnName: 'trendAttendance',
    },
];
